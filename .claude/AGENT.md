Scaffold a new backend microservice named "User Management Service". Define a RESTful API with endpoints POST /auth/login, POST /auth/refresh, GET/POST/PUT/DELETE /users and GET /roles. Design database schemas for users, roles, and permissions; include migrations. Add a Dockerfile and CI/CD pipelines. Document the API contract using OpenAPI.